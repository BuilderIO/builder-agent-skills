# Before / After Examples

## Opener

**Before:** In today's rapidly evolving AI landscape, it's worth noting that open models are fundamentally changing how developers build agentic systems.

**After:** Open models now ship with weights, training data, and inference recipes. You can run them on your own hardware or through an API.

## Binary contrast

**Before:** This isn't just another language model. It's a complete agentic platform designed for the future of work.

**After:** Nemotron 3 ships three model sizes and open training artifacts. You pick Nano for speed or Ultra for hard reasoning tasks.

## False agency

**Before:** The architecture enables seamless multi-agent orchestration at scale.

**After:** You run one agent per thread. Tarsk keeps tool results in context so the next step has what it needs.

## Passive voice

**Before:** API keys are stored locally and never transmitted to third parties.

**After:** Tarsk stores your API keys in local metadata. Requests go directly from your machine to the provider.

## Vague declarative

**Before:** The performance improvements are substantial across a wide range of benchmarks.

**After:** NVIDIA reports 3.3x throughput vs Qwen3-30B-A3B on an H200 at 8K input / 16K output.

## Rule of three

**Before:** Fast inference. Lower cost. Better accuracy.

**After:** Nano activates 3B of 30B parameters per token, which keeps inference cheap on a single GPU.

## Throat-clearing

**Before:** Here's what you need to know about provider support: Tarsk tracks dozens of endpoints automatically.

**After:** Tarsk syncs provider catalogs daily. New models appear in Settings without a release.

## Punchy closer (every paragraph)

**Before:**

> Nemotron 3 uses a hybrid Mamba-Transformer MoE backbone. This design targets agent workloads specifically. Built for the agentic era.

**After:**

> Nemotron 3 uses a hybrid Mamba-Transformer MoE backbone. Mamba layers handle long context in near-linear time; MoE routing keeps active parameters low per token. Agent training used multi-environment RL rather than chat-only fine-tuning.

## Narrator from a distance

**Before:** Nobody wants to manage API keys for every provider separately.

**After:** If you use OpenRouter and Anthropic, you currently juggle two keys and two model pickers. Tarsk puts both in one settings panel.

## Meta-joiner

**Before:** The rest of this post walks through each provider that carries Nemotron 3.

**After:** (Delete. Start the provider section directly.)
